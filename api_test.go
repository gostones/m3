package main

import (
	"testing"
)

func TestP2pIsValid(t *testing.T) {
	// proxy := FreePort()
	// port := FreePort()
	// addr := fmt.Sprintf("127.0.0.1:%v", port)
	// target := fmt.Sprintf("127.0.0.1:%v", proxy)
	// go httpproxy(proxy)
	// go forward(addr, target)

	// t.Logf("addr: %v target: %v", addr, target)

	// ok := p2pIsValid(port)

	// if !ok {
	// 	t.Fail()
	// }
}
